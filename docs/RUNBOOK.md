# Runbook

Filled in during Days 13–14 as the AWS deployment is built, and validated by
having someone unfamiliar with the code execute it. Will cover: deploy,
restart services, read logs, restore DB from backup, and diagnose a slow
report.
